$env.baseNgApp$.config( function( \$routeProvider ) {
    \$routeProvider$routeDefinitions:{ 
    .when( "$it.routePath$", {
        templateUrl : "$it.templateUrl$",
        controller : "$it.controllerName$"
    })}$
});
